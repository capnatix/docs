# GitHub API token used by scripts/fetch-specs.mjs at build time to list
# releases and download each one's openapi.yaml. Optional locally (falls
# back to the unauthenticated 60/hr GitHub API rate limit); in CI this is
# GitHub Actions' own automatic per-run token (secrets.GITHUB_TOKEN) — no
# manual setup needed there.
GITHUB_TOKEN=
